# DevFlx Attendance Frontend

This folder contains a minimal Next.js frontend scaffold with Tailwind theme colors. Install dependencies and run dev server.

Quick start:

```bash
cd frontend
npm install
npm run dev
```
